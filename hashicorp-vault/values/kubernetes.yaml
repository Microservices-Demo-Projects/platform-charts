# Kubernetes-specific overrides for the hashicorp-vault-operator wrapper chart.
# Runs the vault-operator Deployment with 2 replicas (leader-election protected)
# for better availability on standard Kubernetes.

vault-operator:
  replicaCount: 2
  pdb:
    create: true
    minAvailable: 1
