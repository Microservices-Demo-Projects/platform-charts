# Kubernetes-specific overrides for the external-secrets wrapper chart.
# Runs the ESO controller with 2 replicas and enables a PodDisruptionBudget
# for better availability on standard Kubernetes.

external-secrets:
  replicaCount: 2
  podDisruptionBudget:
    enabled: true
