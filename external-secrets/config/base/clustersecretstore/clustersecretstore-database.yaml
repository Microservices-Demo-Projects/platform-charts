# A second store pointed at the same Vault, distinct from local-vault-backend
# only in that it has no `version: v2` set. That setting makes ESO rewrite
# every path through this store into the kv-v2 shape (.../data/...), which is
# wrong for the `database` secrets engine postgres/config/base/external-secret.yaml
# reads from - that mount isn't KV at all, so its paths (e.g.
# database/static-creds/postgres-superuser) must be read literally.
apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: local-vault-backend-database
  labels:
    app.kubernetes.io/name: external-secrets
    app.kubernetes.io/component: cluster-secret-store
spec:
  provider:
    vault:
      server: "https://vault.vault.svc.cluster.local:8200"
      caProvider:
        type: Secret
        name: external-secrets-tls
        key: ca.crt
        namespace: external-secrets
      tls:
        certSecretRef:
          name: external-secrets-tls
          key: tls.crt
          namespace: external-secrets
        keySecretRef:
          name: external-secrets-tls
          key: tls.key
          namespace: external-secrets
      auth:
        kubernetes:
          mountPath: kubernetes
          role: external-secrets
          serviceAccountRef:
            name: external-secrets
            namespace: external-secrets
