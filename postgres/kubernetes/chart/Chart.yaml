apiVersion: v2
name: postgres
description: Wrapper chart for the CloudNativePG (CNPG) operator, which manages PostgreSQL Cluster custom resources.
type: application
version: 0.1.0
appVersion: "1.30.0"

dependencies:
  - name: cloudnative-pg
    version: 0.29.0
    repository: https://cloudnative-pg.github.io/charts
